# OCI provides NTP at 169.254.169.254 (same IP as IMDS). Prefer it so time
# sync works even before the default route is established.
server 169.254.169.254 iburst prefer

# Public fallbacks
pool pool.ntp.org iburst

makestep 1.0 3
rtcsync
driftfile /var/lib/chrony/drift
