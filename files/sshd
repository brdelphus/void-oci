#!/sbin/openrc-run

name="sshd"
description="OpenSSH server"
command="/usr/bin/sshd"
pidfile="/run/sshd.pid"
command_args="-o PidFile=${pidfile}"

depend() {
    need localmount
    after cloud-init
    use logger net
}

start_pre() {
    ssh-keygen -A >/dev/null 2>&1
    checkpath -d /run/sshd
}
